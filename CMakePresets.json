{
  "version": 6,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 28,
    "patch": 0
  },
  "configurePresets": [
    {
      "name": "base",
      "hidden": true,
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "CMAKE_TOOLCHAIN_FILE": "$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake",
        "VCPKG_TARGET_TRIPLET": "x64-windows"
      }
    },
    {
      "name": "default",
      "inherits": "base",
      "displayName": "MSVC x64 (Visual Studio 2022)",
      "description": "Local development build.",
      "generator": "Visual Studio 17 2022",
      "architecture": "x64"
    },
    {
      "name": "media",
      "inherits": "base",
      "displayName": "MSVC x64 with the media layer",
      "description": "Adds FFmpeg and Direct3D 12. The first configure builds FFmpeg from source and takes a while. The generator is left unset so CMake picks whichever Visual Studio is present.",
      "cacheVariables": {
        "CUTLINE_WITH_MEDIA": "ON",
        "CUTLINE_WITH_GPU": "ON",
        "VCPKG_MANIFEST_FEATURES": "media"
      }
    },
    {
      "name": "ui",
      "inherits": "base",
      "displayName": "MSVC x64 with the media layer and Skia",
      "description": "Everything, including the Skia widget backend. The first configure builds both FFmpeg and Skia from source and takes a long time; the UI's own theme and layout code needs neither and builds under the default preset.",
      "cacheVariables": {
        "CUTLINE_WITH_MEDIA": "ON",
        "CUTLINE_WITH_GPU": "ON",
        "CUTLINE_WITH_SKIA": "ON",
        "VCPKG_MANIFEST_FEATURES": "media;gpu"
      }
    },
    {
      "name": "ci",
      "inherits": "base",
      "displayName": "CI (warnings as errors)",
      "description": "Generator is left unset so CMake picks whichever Visual Studio the runner image ships; pinning a version makes CI break when the image moves on.",
      "cacheVariables": {
        "CUTLINE_WARNINGS_AS_ERRORS": "ON"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "debug",
      "configurePreset": "default",
      "configuration": "Debug"
    },
    {
      "name": "release",
      "configurePreset": "default",
      "configuration": "Release"
    },
    {
      "name": "media",
      "configurePreset": "media",
      "configuration": "Release"
    },
    {
      "name": "ui",
      "configurePreset": "ui",
      "configuration": "Release"
    },
    {
      "name": "ci",
      "configurePreset": "ci",
      "configuration": "Release"
    }
  ],
  "testPresets": [
    {
      "name": "debug",
      "configurePreset": "default",
      "configuration": "Debug",
      "output": {
        "outputOnFailure": true
      }
    },
    {
      "name": "ci",
      "configurePreset": "ci",
      "configuration": "Release",
      "output": {
        "outputOnFailure": true
      }
    }
  ]
}
